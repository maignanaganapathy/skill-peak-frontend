import React from "react";
import { Button } from "@mui/material";

const CreateQuizButton: React.FC = () => (
  <Button variant="contained" color="primary">
    Create New Quiz
  </Button>
);

export default CreateQuizButton;
